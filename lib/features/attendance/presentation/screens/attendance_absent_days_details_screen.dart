import 'package:flutter/material.dart';

class AttendanceAbsentDaysDetailsScreen extends StatelessWidget {
  const AttendanceAbsentDaysDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hi attendance Absent Days Details"),
      ),
    );
  }
}
