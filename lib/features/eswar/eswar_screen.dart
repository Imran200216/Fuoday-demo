import 'package:flutter/material.dart';

class EswarScreen extends StatelessWidget {
  const EswarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
